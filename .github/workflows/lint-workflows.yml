name: Lint workflows

on:
  push:
    branches: ['**']
    paths:
      - '.github/workflows/**'
  pull_request:
    paths:
      - '.github/workflows/**'

permissions:
  contents: read

concurrency:
  group: lint-workflows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Download actionlint
        env:
          # actionlint ships as a binary, not an action, so it cannot be pinned
          # to a commit SHA. Bumped by hand; the checksum is copied from the
          # release's own *_checksums.txt, so a substituted binary is refused
          # rather than silently trusted with this job's token.
          ACTIONLINT_VERSION: 1.7.12
          ACTIONLINT_SHA256: 8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8
        run: |
          set -euo pipefail
          curl -fsSL -o actionlint.tar.gz \
            "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz"
          echo "${ACTIONLINT_SHA256}  actionlint.tar.gz" | sha256sum -c -
          tar xzf actionlint.tar.gz actionlint

      - name: Run actionlint
        # actionlint shells out to shellcheck for `run:` blocks and silently
        # skips that pass when shellcheck is not installed. The GitHub runner
        # has it; a bare container may not, so a local run can come back clean
        # on a workflow that fails here. Install shellcheck before trusting a
        # local actionlint run.
        run: ./actionlint -color
